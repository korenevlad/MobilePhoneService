@model Manufacturer

<div class="card shadow border-0 mt-5">
    <div class="card-header bg-primary bg-gradient ml-0 py-2">
        <div class="row">
            <div class="col-12 text-center">
                <h3 class="text-white py-2"> @(Model.manufacturer_id != 0 ? "Обновление" : "Добавление") производителя</h3>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post" class="row">
            <input asp-for="manufacturer_id" hidden />
            <div class="border p-3">
                <div asp-validation-summary="All"></div>
                <div class="py-2 col-12">
                    <label asp-for="manufacturer_name" class="form-label ms-2">Название</label>
                    <input asp-for="manufacturer_name" autocomplete="off" class="form-control border-0 shadow" placeholder="Название" />
                    <span asp-validation-for="manufacturer_name" class="text-danger"></span>
                </div>
                <div class="py-2 col-12">
                    <label asp-for="country" class="form-label mt-4 ms-2">Страна</label>
                    <input asp-for="country" autocomplete="off" class="form-control border-0 shadow mb-4" placeholder="Страна" />
                    <span asp-validation-for="country" class="text-danger"></span>
                </div>

                <div class="row pt-2">
                    <div class="col-6 col-md-3">
                        @if (Model.manufacturer_id != 0)
                        {
                            <button type="submit" class="btn btn-outline-success border form-control"><i class="bi bi-pencil-fill"></i>&nbsp; Обновить</button>
                        }
                        else
                        {
                            <button type="submit" class="btn btn-outline-success border form-control"><i class="bi bi-plus-square"></i> &nbsp; Добавить</button>
                        }
                    </div>
                    <div class="col-6 col-md-3">
                        <a asp-controller="Manufacturer" asp-action="Index" class="btn btn-outline-danger border form-control">
                            <i class="bi bi-arrow-bar-left"></i> &nbsp;Назад к списку
                        </a>
                    </div>
                </div>
            </div>
        </form>

    </div>
</div>

@section Scripts{
    <script>
        tinymce.init({
            selector: 'textarea',
            plugins: 'anchor autolink charmap codesample emoticons  lists   table visualblocks wordcount',
            toolbar: 'undo redo | blocks fontfamily fontsize | bold italic underline strikethrough |  align lineheight | numlist bullist indent outdent',
        });
    </script>
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}








