@model Cpu

<div class="card shadow border-0 mt-5">
    <div class="card-header bg-primary bg-gradient ml-0 py-2">
        <div class="row">
            <div class="col-12 text-center">
                <h3 class="text-white py-2"> @(Model.cpu_id != 0 ? "Обновление" : "Добавление") процессора</h3>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post" class="row">
            <input asp-for="cpu_id" hidden />
            <div class="border p-3">
                <div asp-validation-summary="All"></div>
                <div class="py-2 col-12">
                    <label asp-for="model" class="form-label ms-2">Название</label>
                    <input asp-for="model" autocomplete="off" class="form-control border-1 shadow" placeholder="Название" />
                    <span asp-validation-for="model" class="text-danger"></span>
                </div>
                <div class="py-2 col-12">
                    <label asp-for="amount_cernels" class="form-label mt-4 ms-2">Количество ядер</label>
                    <input asp-for="amount_cernels" autocomplete="off" class="form-control border-0 shadow" placeholder="Количество ядер" />
                    <span asp-validation-for="amount_cernels" class="text-danger"></span>
                </div>
                <div class="py-2 col-12">
                    <label asp-for="frequency" class="form-label mt-4 ms-2">Частота</label>
                    <input asp-for="frequency" autocomplete="off" class="form-control border-0 shadow mb-4" placeholder="Частота" />
                    <span asp-validation-for="frequency" class="text-danger"></span>
                </div>
                <div class="row pt-2">
                    <div class="col-6 col-md-3">
                        @if (Model.cpu_id != 0)
                        {
                            <button type="submit" class="btn btn-outline-success border form-control"><i class="bi bi-pencil-fill"></i>&nbsp; Обновить</button>
                        }
                        else
                        {
                            <button type="submit" class="btn btn-outline-success border form-control"><i class="bi bi-plus-square"></i> &nbsp; Добавить</button>
                        }
                    </div>
                    <div class="col-6 col-md-3">
                        <a asp-controller="Cpu" asp-action="Index" class="btn btn-outline-danger border form-control">
                            <i class="bi bi-arrow-bar-left"></i> &nbsp;Назад к списку
                        </a>
                    </div>
                </div>
            </div>
        </form>

    </div>
</div>

@section Scripts{
    <script>
        tinymce.init({
            selector: 'textarea',
            plugins: 'anchor autolink charmap codesample emoticons  lists   table visualblocks wordcount',
            toolbar: 'undo redo | blocks fontfamily fontsize | bold italic underline strikethrough |  align lineheight | numlist bullist indent outdent',
        });
    </script>
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}








